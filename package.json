{
  "name": "@simnalamburt/monorepo",
  "private": true,
  "license": "(Apache-2.0 OR MIT)",
  "dependencies": {
    "@vitest/coverage-v8": "^4.0.17",
    "typescript": "catalog:",
    "vitest": "^4.0.17"
  },
  "scripts": {
    "build": "pnpm -r build",
    "doc": "pnpm -r doc",
    "test": "pnpm -r test && vitest run && echo 'TODO: run formatter and linter'",
    "coverage": "vitest run --coverage",
    "fmt": "echo 'TODO: run formatter'"
  },
  "packageManager": "pnpm@10.28.0"
}
