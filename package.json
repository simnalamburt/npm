{
  "name": "@simnalamburt/monorepo",
  "private": true,
  "license": "(Apache-2.0 OR MIT)",
  "scripts": {
    "build": "pnpm -r build",
    "ci": "pnpm -r check && vitest run && oxlint && oxfmt --check",
    "coverage": "vitest run --coverage",
    "doc": "pnpm -r doc",
    "fix": "oxlint --fix",
    "fmt": "oxfmt"
  },
  "dependencies": {
    "@vitest/coverage-v8": "^4.0.17",
    "oxfmt": "^0.32.0",
    "oxlint": "^1.47.0",
    "typescript": "catalog:",
    "vitest": "^4.0.17"
  },
  "packageManager": "pnpm@10.28.0"
}
